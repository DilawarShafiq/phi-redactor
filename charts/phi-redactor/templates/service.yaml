apiVersion: v1
kind: Service
metadata:
  name: {{ include "phi-redactor.fullname" . }}
  labels:
    {{- include "phi-redactor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phi-redactor.selectorLabels" . | nindent 4 }}
